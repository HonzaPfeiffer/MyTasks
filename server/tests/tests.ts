import assert from 'assert'
import { expect } from 'chai'

describe('Integration tests for tasks module', () => {
    it('With sending request task should be created', async () => {
        
    })
})